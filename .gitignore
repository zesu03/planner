node_modules
dist
.env
.env.local
.vercel
.env*.local

# Plaintext backup artifact — only the encrypted .gpg is ever committed
_backup.json

# Rollback copy of the live rules, written by scripts/deploy-rules.mjs
firestore.rules.live-backup
